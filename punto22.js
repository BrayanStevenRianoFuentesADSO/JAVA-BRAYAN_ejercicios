/*Escribir un programa que muestre el pago de una llamada telefónica sabiendo que
cada minuto cuesta $355 pesos y un IVA 20%.*/

let minutos = prompt("numero de minutos en llamada")

iva = 0.2
val_min = (minutos * 355)
costo_fin = (val_min + valmin * iva)
document.write("el costo por la llamada es de ")